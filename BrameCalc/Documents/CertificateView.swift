import SwiftUI

struct CertificateView: View {
    var body: some View {
        Text("Certificat de bon montage")
            .font(.largeTitle)
            .padding()
        
        Spacer()
    }
}

