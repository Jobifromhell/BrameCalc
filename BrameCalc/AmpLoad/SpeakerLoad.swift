import SwiftUI


class SpeakerLoad {
    let speaker: String
    let quantity: Int
    
    init(speaker: String, quantity: Int) {
        self.speaker = speaker
        self.quantity = quantity
    }
}
