import SwiftUI

struct OrganigrammeView: View {
    var body: some View {
        Text("Organigramme")
            .font(.largeTitle)
            .padding()
        
        Spacer()
    }
}

struct OrganigrammeView_Previews: PreviewProvider {
    static var previews: some View {
        OrganigrammeView()
    }
}
