import SwiftUI

struct AVProjectView: View {
    var body: some View {
        Text("Projet AV")
            .font(.largeTitle)
            .padding()
        
        Spacer()
    }
}

struct AVProjectView_Previews: PreviewProvider {
    static var previews: some View {
        AVProjectView()
    }
}
